class ChatGPTManager {
    static async sendPrompt() {
        //Needs to connect with ChatGPT API and send the user's prompt 
    }   

    static async newAnswer() {
        //Needs to connect with ChatGPT API and send the user's prompt and return other response
    }
}