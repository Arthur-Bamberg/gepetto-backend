class User {
    static registerUser(name, email, phone, password) {
        this.name = name;
        this.email = email;
        this.phone = phone;
        this.password = password;
    }

    static validateLogin(email, password) {
        
    }

    static deleteAccount(idUser) {

    }
}